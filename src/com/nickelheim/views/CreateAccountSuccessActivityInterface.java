package com.nickelheim.views;

import android.view.View;

/**
 * Interface for CreateAccountSuccess Activity.
 * 
 * @author Nickelheim
 *
 */
public interface CreateAccountSuccessActivityInterface {
    /**
     * Method called when button is clicked so that user can go to a new
     * activity.
     * 
     * @param view is the activity
     */
    void viewReport(View view);
}
